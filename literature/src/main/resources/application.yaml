server:
  port: 8081

spring:
  datasource:
    username: root
    password: rBq5TScFUt796Wo34d
    url: jdbc:mysql://localhost:3306/scholar?serverTimezone=UTC&useUnicode=true&characterEncoding=utf-8

mybatis:
  type-aliases-package: com.scholar.literature.pojo
  mapper-locations: classpath:mapper/*.xml

