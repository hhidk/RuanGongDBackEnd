<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scholar.profile.mapper.ReportMapper">

    <insert id="addReport" parameterType="Report">
        insert
        into Report(type, content, status, reporteeID, reporterID)
        values(#{type}, #{content}, #{status}, #{reporteeID}, #{reporterID})
    </insert>

</mapper>